# PROJECT_STRUCTURE.md — erikrmiller.com

Established: 2026-07-09 (workspace audit). Single source of truth for what lives
where and which folders are canonical vs. stale.

---

## 1. Canonical repository

- **Local:** `~/Documents/erikrmiller-site/`  (may display as `Documents:erikrmiller-site:`)
- **GitHub:** `https://github.com/646RX-hub/erikrmiller.com`
- **Branch:** `main` · **Live:** `https://erikrmiller.com` · **Host:** GitHub Pages (`CNAME` = erikrmiller.com)

This is the **only** folder where website development occurs.

---

## 2. Repository layout (top level)

```
erikrmiller-site/
├── index.html                 # homepage
├── about.html / about/        # about
├── blog.html + blog/          # 34 posts, each blog/<slug>/index.html
├── frameworks/                # framework hub + one dir per framework
│   ├── index.html, frameworks.css, FRAMEWORKS-STRATEGY.md
│   └── <framework-slug>/index.html (+ SVGs, OG images)
├── resources/                 # interactive resources / lead magnets
│   ├── index.html
│   └── <resource-slug>/index.html (e.g. machine-customer-readiness-index)
├── icp/                       # ICP resource
├── ebooks/                    # downloadable ebooks
├── visual-library/            # shared visual assets
├── partials/                  # SSOT fragments: nav.html, footer.html
├── assets/                    # images, nav, footer, search
│   └── search/                # search-index.json, search.js, search.css
├── scripts/                   # build + validation (see below)
├── docs/                      # editorial + audit docs
├── .github/workflows/         # CI validators + indexnow
├── CNAME                      # erikrmiller.com
├── robots.txt                 # points to sitemap
├── sitemap.xml
├── ERM-LAUNCH-STANDARD.md     # flagship launch checklist
├── CLAUDE.md / PROJECT_STRUCTURE.md / WORKFLOW.md   # governance (this set)
└── *.pdf                      # published framework/workbook PDFs
```

### scripts/
- `build-search-index.py` — regenerate `assets/search/search-index.json`
- `validate-search-index.py` — CI guard for the search index
- `build-nav.py` / `validate-nav.py` — nav SSOT from `partials/nav.html`
- `build-footer.py` / `validate-footer.py` — footer SSOT from `partials/footer.html`
- `validate-blog-cards.py` — blog card consistency (+ `blog-card-exemptions.txt`)
- `install-search.py` — search wiring helper

### Ignored / never deploy (from `.gitignore`)
`content-tracker.xlsx`, `COWORK_BRIEFING.md`, `_drafts/`, `.DS_Store`,
`*.backup`, `*.bak`, `index-*-working.html`, `*-preview.html`,
`commit-framework-launch.sh`, `**/LAUNCH-PACKAGE.md`, `.indexnow/`.

---

## 3. Other folders on this machine (NOT the website repo)

### `~/Documents/Claude/Projects/My Website`  — STALE / LEGACY COPY
- **Not a Git repository** (no `.git`). Cannot be committed or deployed.
- A near-duplicate of the site (~227 files) that has **drifted** from canonical:
  - `robots.txt` differs (adds `Disallow: /_archive/`; newer mtime, never in repo)
  - `index.html` and `about.html` differ from the committed versions
  - Contains working/preview clutter not in the repo: `_archive/`, `wip/`,
    `preview/`, `.preview/`, `index-icp-working.html`, loose `.bak` files
- Recently touched (files dated as late as 2026-07-09), which is the exact risk:
  edits made here never reach GitHub or the live site.
- **Action: do not edit. Quarantine, then reconcile any wanted drift into the
  repo before removing.** (See WORKFLOW.md §5 and the audit report.)

### `~/Documents/Claude/Projects/ERM Content Production Engine`  — SEPARATE PROJECT (content OS)
- Not a website copy. A content-operations system (Operating System, Sources of
  Truth, Templates, Campaigns, Production, Research OS, etc.).
- Hosts **Command Center** and **Social Studio**:
  - `09_Research_OS/ERM_Command_Center.html`, `ERM_Social_Studio.html`
  - Generated by `09_Research_OS/Automation/build_command_center.py`
- **Command Center path (FIXED 2026-07-09):** `build_command_center.py`
  (line 19) previously set `SITE = ROOT.parent / "My Website"`, reading the
  **stale** copy. It now reads the canonical repo:
  `SITE = Path.home() / "Documents" / "erikrmiller-site"`. Regenerate the
  Command Center on Erik's machine (`python3 build_command_center.py`) to apply
  the corrected sitemap/URL counts to the live views.

---

## 4. Canonical vs. stale — summary

| Folder | Git repo? | Role | Status |
|---|---|---|---|
| `~/Documents/erikrmiller-site/` | Yes (origin: 646RX-hub/erikrmiller.com) | Live website | **CANONICAL** |
| `~/Documents/Claude/Projects/My Website` | No | Old website copy | **STALE — do not use** |
| `~/Documents/Claude/Projects/ERM Content Production Engine` | No | Content ops / Command Center / Social Studio | Separate project (keep) |

> Scope note: this audit covered the folders connected to this workspace. A
> full-disk scan was not performed. Never assume another website copy exists
> unless a fresh audit proves otherwise.
